<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="#F7F9FB"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Logo FSTG -->
    <ImageView
        android:id="@+id/logoFstg"
        android:src="@drawable/fstg_logo"
        android:contentDescription="Logo FSTG Marrakech"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_marginBottom="16dp" />

    <!-- Titre -->
    <TextView
        android:id="@+id/txtTitle"
        android:text="FSTG Marrakech"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp" />

    <!-- Sous-titre -->
    <TextView
        android:id="@+id/txtSubtitle"
        android:text="Faculté des Sciences et Techniques"
        android:textSize="16sp"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp" />

    <!-- Bouton Login -->
    <Button
        android:id="@+id/btnLogin"
        android:text="Se connecter"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp" />

    <!-- Bouton Register -->
    <Button
        android:id="@+id/btnRegister"
        android:text="Créer un compte"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp" />

    <!-- Texte services -->
    <TextView
        android:id="@+id/txtPublicServices"
        android:text="Accéder aux services publics"
        android:textSize="14sp"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="4dp" />

    <!-- Liens rapides -->
    <LinearLayout
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/txtNews"
            android:text="Actualités   "
            android:textSize="14sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/txtAnnouncements"
            android:text="Annonces   "
            android:textSize="14sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/txtContacts"
            android:text="Contacts"
            android:textSize="14sp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </LinearLayout>

</LinearLayout>
